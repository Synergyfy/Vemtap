## Completed (this session)
- **2-step invite wizard**: full redesign with step indicator, mobile-first layout (`items-end sm:items-center`, `sm:max-w-xl`)
- **Role suggestion chips** (Manager, Cashier, Supervisor, Inventory, Marketing, Support, Accountant, HR) in invite step 1 & edit modal
- **Tooltip `?` icons** on each form field (first/last name, email, phone, role, permissions)
- **Single-accordion permission tree**: `toggleSection` sets only one open at a time (closes if clicked again), starts all collapsed
- **Section labels** use `section.label` from `NAVIGATION_SECTIONS` (e.g. "Analytics", "Get Customers") fallback to item labels
- **Wider layout**: both modals use `w-full sm:max-w-xl` with bigger padding (`p-5 sm:p-7`), larger inputs (`h-12`), bigger buttons (`h-14`)
- **Edit modal**: same permission tree + role suggestions + tooltips as invite, matched mobile/responsive design
- **Clean TypeScript compile** (`npx tsc --noEmit` passes)
- Committed to `dev` locally (push timed out)

## Todo
1. Push to origin/dev (retry when network is stable)
2. Merge into tobee if needed
